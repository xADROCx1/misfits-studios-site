# Secrets — never commit
scripts/.env
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/

# Build artifacts (if we add a build step later)
node_modules/
dist/

# Paid inventory — plugin source files belong in Lemon Squeezy as download assets,
# NOT in a public repo (would let buyers clone instead of purchase).
(XADROCX) PLUGINS/

# Design scratch / reference files that don't need to ship
Misfits studio Logo.psd
stitch.zip
stitch/
stitch_misfits_studios_marketplace/
universal_upscale_0_*.png
SETUP-LEGACY-gumroad.md
